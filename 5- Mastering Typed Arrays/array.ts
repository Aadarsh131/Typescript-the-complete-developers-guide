const company = ['google','microsoft','apple',4,new Date(),[['f33'],['f34'],['f32']]]

const stringCompany = ['hcl','tcs','cadbury']
const cassete: string[][] = [];

const specificCompany = company [4]

// stringCompany.push([400]) //even though the 'stringCompany' is inferecing the types from its implementation, if after the 1st initializatioin we try to change its type, then it throws an error (NOTE: it would work perfectly fine in javascript though)



